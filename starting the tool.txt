# Action directly at the root

# Generate Prisma types (Creates TypeScript types from your database schema)
npm run prisma:generate

# Start development (Runs all projects in development mode with auto-reload)
npm run dev

# Build the application (Compiles all projects for production deployment)
npm run build

# Apply migrations (Updates your database schema with pending migrations)
npm run prisma:migrate
